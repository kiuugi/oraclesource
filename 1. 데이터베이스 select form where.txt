1. 데이터베이스 ->관리를 위해 SQL을 사용
SQL = DBMS 질의 언어
 데이터 조회 - select
	      행 단위 조회 (특정 행만 선별하여 조회) : select *(전체) form 테이블명 where ~~~
	      열 단위 조회 : select empno, deptno (필드명) form 테이블명 where ~~~~~~~
	      where 절이 필요한 경우
			 조건 명시 - ~와 같다, ~보다 크다, ~와 ~ 사이에 있다 ... .
			 산술연산자 기호 사용, =, >=, <=, != ... .

날짜, 문자 데이터는 (' ') 붙여서 사용
